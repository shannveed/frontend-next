import UpdateMoviesClient from '../../components/dashboard/UpdateMoviesClient';

export const metadata = {
  title: 'Update Movies',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <UpdateMoviesClient />;
}
