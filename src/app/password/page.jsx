import PasswordClient from '../../components/dashboard/PasswordClient';

export const metadata = {
  title: 'Change Password',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <PasswordClient />;
}
